{
  "name": "taskrouter-demo",
  "version": "0.0.11",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "npm version patch && twilio serverless:deploy --force"
  },
  "dependencies": {
    "twilio": "^3.56",
    "@twilio/runtime-handler": "1.3.0"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "16"
  }
}
